quarkus.stork.product-service.service-discovery.type=static
quarkus.stork.product-service.service-discovery.address-list=${gateway.test.upstream}
quarkus.stork.order-service.service-discovery.type=static
quarkus.stork.order-service.service-discovery.address-list=${gateway.test.upstream}
gateway.consul-ui.url=${gateway.test.consul-ui}
quarkus.otel.traces.exporter=cdi
quarkus.otel.traces.enabled=true
quarkus.otel.traces.sampler=always_on
quarkus.otel.traces.suppress-non-application-uris=false
quarkus.oidc.client-id=quarkus-service-app
quarkus.oidc.credentials.secret=secret
